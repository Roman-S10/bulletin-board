{% extends 'layout.html.twig' %}

{% block title %}
    {% if id %}
        Редактирование города<small>[{{ id }}]</small>
    {% else %}
        Добавление города
    {% endif %}
{% endblock %}

{% block content %}
    {{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.name, { label: 'Наименование', attr: { class: 'form-control' }  }) }}
    </div>
    <div class="form-group">
        {{ form_row(form.area, { label: 'Область', attr: { class: 'form-control' }  }) }}
    </div>
    {{ form_row(form.save, { attr: { class: 'btn btn-primary btn-lg btn-block' }}) }}
    {{ form_end(form) }}
{% endblock %}